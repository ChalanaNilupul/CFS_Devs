setTimeout(() => {

    const n = document.getElementById('n');
    const i = document.getElementById('i');
    const l = document.getElementById('l');
    const u = document.getElementById('u');
    const p = document.getElementById('p');
    const u2= document.getElementById('u2');
    const l2 = document.getElementById('l2');

   
    setTimeout(() => (n.style.opacity="1"), 500);
    setTimeout(() => (i.style.opacity = '1'), 700);
    setTimeout(() => (l.style.opacity = '1'), 900);
    setTimeout(() => (u.style.opacity = '1'), 1100);
    setTimeout(() => (p.style.opacity = '1'), 1300);
    setTimeout(() => (u2.style.opacity = '1'), 1500);
    setTimeout(() => (l2.style.opacity = '1'), 1700);

}, 300);